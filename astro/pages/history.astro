---
// GET /history — artifact history dashboard (always fresh, never cached).
import HistoryDashboard from '../components/HistoryDashboard.astro';
import { VIEWER_SECURITY_HEADERS } from '../../worker/headers.js';

export const prerender = false;

for (const [k, v] of Object.entries(VIEWER_SECURITY_HEADERS)) {
  Astro.response.headers.set(k, v);
}
Astro.response.headers.set('Cache-Control', 'no-cache, no-store, must-revalidate');
---
<HistoryDashboard />
