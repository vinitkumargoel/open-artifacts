# Server Settings
PORT=3008
NODE_ENV=production
BASE_URL=http://localhost:3008

# Publisher Access Token (Generate with: openssl rand -hex 24)
ARTIFACT_ACCESS_TOKEN=your_secure_random_token_here

# Storage & Database
MONGO_URI=mongodb://127.0.0.1:27017/open_artifacts
STORAGE_PATH=./data/artifacts
MAX_FILE_SIZE_MB=25

# Rate Limits (Requests per minute)
UPLOAD_RATE_LIMIT_PER_MIN=30
# Applied per visitor IP, not per server, so this can be generous.
READ_RATE_LIMIT_PER_MIN=500
# Upstream hops allowed to set X-Forwarded-For / CF-Connecting-IP.
TRUST_PROXY=loopback, linklocal, uniquelocal
